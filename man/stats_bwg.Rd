% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/stats-bwg.R
\name{stats_bwg}
\alias{stats_bwg}
\title{Calculate interval statistics}
\usage{
stats_bwg(
  x,
  chrom,
  start,
  end,
  n_bins = 1L,
  stat = c("mean", "stdev", "max", "min", "coverage", "sum"),
  sample_ids = NULL,
  use_zoom = TRUE
)
}
\arguments{
\item{x}{A validated \code{BwgTrack} object.}

\item{chrom}{Chromosome name.}

\item{start}{Start coordinate, 1-based closed.}

\item{end}{End coordinate, 1-based closed.}

\item{n_bins}{Number of equal-width bins.}

\item{stat}{Statistic: \code{mean}, \code{stdev}, \code{max}, \code{min}, \code{coverage}, or \code{sum}.}

\item{sample_ids}{Optional sample IDs. Defaults to all samples.}

\item{use_zoom}{Whether lazy BigWig inputs may use zoom summary levels.
Zoom results may be approximate when query bins partially overlap stored
zoom windows. Use \code{FALSE} for exact values.}
}
\value{
A data.table with one row per sample and genomic bin.
}
\description{
Calculates coverage-aware statistics over equal-width genomic bins from a
standardized \code{BwgTrack}. Memory-mode tracks use full-resolution signal.
Lazy BigWig tracks may use stored zoom summaries when \code{use_zoom = TRUE}.
Exact lazy-BigWig calculations stream overlapping full-resolution blocks
directly into bin accumulators without materializing an interval table. When
all chromosome lengths for a selected sample are known in \code{seqinfo}, query ends
are clipped to the known chromosome length and unknown chromosomes are
rejected. Queries starting beyond a known chromosome return a stable empty
statistics table.
}
\details{
Bins are constructed after known chromosome bounds are applied. Exact lazy
BigWig statistics stream indexed full-resolution blocks. Zoom-aware results
expose their source and stored reduction level in \code{source} and \code{resolution}.
}
\examples{
bw_file <- system.file(
  "extdata", "bwtools_example.bigwig",
  package = "bwTools", mustWork = TRUE
)
x <- read_bwg(bw_file, sample_ids = "example")
stats_bwg(
  x, "chr1", 1L, 1000L,
  n_bins = 10L, stat = "mean", use_zoom = FALSE
)
}
\seealso{
\code{\link[=retrieve_bwg]{retrieve_bwg()}}, \code{\link[=zoominfo_bwg]{zoominfo_bwg()}}
}
