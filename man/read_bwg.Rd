% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/read-bwg.R
\name{read_bwg}
\alias{read_bwg}
\title{Read genomic signal tracks}
\usage{
read_bwg(
  files,
  format = c("auto", "bigwig", "wig", "bedgraph"),
  sample_ids = NULL,
  strand = "*",
  mode = c("auto", "memory", "lazy")
)
}
\arguments{
\item{files}{One or more local BigWig, WIG, or bedGraph files.}

\item{format}{Input format. \code{auto} detects each file independently.}

\item{sample_ids}{Optional sample IDs. Defaults to input file stems.}

\item{strand}{Strand labels associated with input files. Values must be \code{+},
\code{-}, or \code{*}.}

\item{mode}{Loading mode. \code{auto} uses lazy access when all inputs are BigWig
and memory mode otherwise. \code{lazy} is supported for BigWig inputs only.}
}
\value{
A validated \code{BwgTrack} object.
}
\description{
Reads one or more local BigWig, WIG, or bedGraph files into the standardized
bwTools \code{BwgTrack} contract.
}
\details{
Public signal coordinates are converted to 1-based closed coordinates on
input. BigWig-only input is lazy in \code{mode = "auto"}; WIG, bedGraph, and
mixed-format input are materialized in memory. Sample IDs must be unique
within one call.
}
\examples{
bw_file <- system.file(
  "extdata", "bwtools_example.bigwig",
  package = "bwTools", mustWork = TRUE
)
x <- read_bwg(bw_file, sample_ids = "example")
summary_bwg(x)
}
\seealso{
\code{\link[=detect_bwg_format]{detect_bwg_format()}}, \code{\link[=write_bwg]{write_bwg()}}, \code{\link[=bwg_track]{bwg_track()}}
}
