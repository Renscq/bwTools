% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/subset-bwg.R
\name{subset_bwg}
\alias{subset_bwg}
\title{Subset genomic signal tracks}
\usage{
subset_bwg(
  object,
  regions,
  outdir = NULL,
  format = c("bigwig", "wig", "bedgraph"),
  samples = NULL,
  chrom_sizes = NULL,
  overwrite = FALSE,
  compress = FALSE,
  zoom = TRUE,
  max_zoom_levels = 10L,
  empty = c("error", "skip")
)
}
\arguments{
\item{object}{A \code{BwgTrack}-compatible object.}

\item{regions}{A data.frame or data.table containing \code{chrom}, \code{start},
and \code{end} columns in 1-based closed coordinates. Overlapping or directly
adjacent regions are merged before extraction.}

\item{outdir}{Optional output directory. If \code{NULL}, return an in-memory
subset without writing files.}

\item{format}{Output format when \code{outdir} is supplied: \code{bigwig},
\code{wig}, or \code{bedgraph}.}

\item{samples}{Optional sample IDs. By default all samples are retained.}

\item{chrom_sizes}{Optional chromosome sizes passed to \code{write_bwg()} for
BigWig output. Complete lengths are otherwise taken from subset \code{seqinfo}
when available.}

\item{overwrite}{Whether existing output files may be replaced.}

\item{compress}{Whether WIG or bedGraph output should be gzip-compressed.}

\item{zoom}{Whether BigWig output should include zoom summary levels.}

\item{max_zoom_levels}{Maximum number of BigWig zoom levels to create.}

\item{empty}{File-output behavior for selected samples with no overlapping
signal: \code{error} stops before writing, while \code{skip} records the sample
as empty in the returned manifest and writes only non-empty samples.}
}
\value{
If \code{outdir = NULL}, a \code{BwgTrack}-compatible in-memory object.
Otherwise, invisibly returns a data.table with \code{sample_id}, \code{file},
\code{format}, \code{status}, and \code{intervals}.
}
\description{
Extracts the union of one or more genomic regions from a \code{BwgTrack}
object. Intervals crossing a requested boundary are clipped to that boundary.
For lazy BigWig inputs, only indexed BigWig blocks overlapping the requested
regions are decoded, so memory use scales with the selected subset rather than
the complete source track.

When \code{outdir = NULL}, an in-memory \code{BwgTrack} subset is returned.
When an output directory is supplied, the subset is written as BigWig, WIG, or
bedGraph using \code{write_bwg()} and a file manifest is returned invisibly.
}
