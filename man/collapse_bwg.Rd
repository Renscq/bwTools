% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/merge-bwg.R
\name{collapse_bwg}
\alias{collapse_bwg}
\title{Aggregate overlapping genomic signal tracks}
\usage{
collapse_bwg(
  ...,
  tracks = NULL,
  method = c("sum", "mean"),
  by = c("sample", "all"),
  groups = NULL,
  output_sample = NULL,
  missing = c("zero", "ignore"),
  strand = c("separate", "ignore"),
  drop_zero = TRUE,
  merge_adjacent = TRUE
)
}
\arguments{
\item{...}{Two or more \code{BwgTrack}-compatible objects.}

\item{tracks}{Optional list of \code{BwgTrack}-compatible objects. Use either \code{...} or \code{tracks}.}

\item{method}{Aggregation method: \code{sum} or \code{mean}.}

\item{by}{Default grouping rule when \code{groups} is not supplied: \code{sample} preserves sample IDs; \code{all} combines all samples.}

\item{groups}{Optional named character vector or table with \code{sample_id} and \code{group_id} columns.}

\item{output_sample}{Output sample ID used when \code{by = "all"}. Defaults to \code{collapsed}.}

\item{missing}{Mean denominator rule: \code{zero} treats uncovered members as zero; \code{ignore} excludes uncovered members. Ignored for \code{method = "sum"}.}

\item{strand}{Strand aggregation rule. \code{separate} keeps \code{+}, \code{-}, and \code{*} signals separate; \code{ignore} combines strands.}

\item{drop_zero}{Whether zero-valued output segments should be omitted.}

\item{merge_adjacent}{Whether directly adjacent output segments with equal values should be merged.}
}
\value{
A memory-mode \code{BwgTrack} containing aggregated signal.
}
\description{
Collapses overlapping signal from two or more \code{BwgTrack} objects using arithmetic aggregation. Partially overlapping intervals are split at every input boundary before values are calculated, so aggregation does not require identical input interval coordinates.
}
\details{
\code{by = "sample"} aggregates repeated instances of the same sample across input tracks and preserves the sample ID. \code{by = "all"} aggregates all input samples into one output sample. \code{groups} provides an explicit sample-to-group mapping for treatment- or replicate-level aggregation.

For means, \code{missing = "zero"} treats uncovered members as zero, whereas \code{missing = "ignore"} averages only members that cover a given atomic segment.
}
