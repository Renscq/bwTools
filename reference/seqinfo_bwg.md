# Return sample-specific chromosome metadata

Return sample-specific chromosome metadata

## Usage

``` r
seqinfo_bwg(x, sample_ids = NULL)
```

## Arguments

- x:

  A validated `BwgTrack` object.

- sample_ids:

  Optional sample IDs. Defaults to all samples.

## Value

A standardized chromosome metadata data.table.
